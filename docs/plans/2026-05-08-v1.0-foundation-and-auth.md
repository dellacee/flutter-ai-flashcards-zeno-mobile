# V1.0 Foundation + Auth + Manual CRUD — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship một Flutter app chạy được trên Android + iOS với auth (Google/Apple/Email), tạo deck/card thủ công, upload PDF lên Firebase Storage. **Chưa có AI / chưa có review.** Mục đích: foundation vững + xác minh tech stack hoạt động trước khi đầu tư vào AI pipeline.

**Architecture:** Flutter app (Dart 3.x) clean-architecture-lite (data/domain/presentation per feature), Riverpod 2.x state, GoRouter routing, Material 3 Expressive theme, Isar v4 cache offline, Firebase Auth + Firestore + Storage backend. Không có custom backend ở plan này — Firestore SDK trực tiếp.

**Tech Stack:** Flutter 3.x, Dart 3.x, Riverpod 2.x + riverpod_generator, GoRouter, freezed + json_serializable, Isar v4, Firebase Auth/Firestore/Storage/App Check/Analytics, Sentry, very_good_analysis, GitHub Actions.

**Spec reference:** [docs/specs/2026-05-08-ai-learning-companion-design.md](../specs/2026-05-08-ai-learning-companion-design.md)

---

## Pre-flight checklist (do trước khi bắt đầu Task 1)

- [ ] Node.js ≥20 cài sẵn (cho FlutterFire CLI)
- [ ] Flutter SDK ≥3.24 — `flutter doctor` PASS hết
- [ ] Android Studio + Android emulator (Pixel 7 API 34)
- [ ] (Optional) Xcode + iOS simulator nếu trên Mac (skip nếu Windows)
- [ ] Git cài + có account GitHub
- [ ] Tài khoản Google Cloud / Firebase (cùng email với Google Sign-In test)
- [ ] Tài khoản Apple Developer (chỉ cần khi build iOS — defer được tới V1.2)

---

## File structure (sau khi xong plan này)

```
D:\mobile\
├── .github/workflows/
│   └── ci.yml                              # analyze + test
├── .gitignore
├── README.md
├── app/                                    # Flutter app
│   ├── pubspec.yaml
│   ├── analysis_options.yaml
│   ├── firebase.json                       # FlutterFire config
│   ├── android/                            # platform code
│   ├── ios/
│   ├── lib/
│   │   ├── main.dart
│   │   ├── app.dart                        # ZenoApp root widget
│   │   ├── core/
│   │   │   ├── di/
│   │   │   │   └── providers.dart          # global Riverpod providers
│   │   │   ├── routing/
│   │   │   │   ├── app_router.dart         # GoRouter config
│   │   │   │   └── app_router.g.dart       # generated
│   │   │   ├── theme/
│   │   │   │   ├── app_theme.dart          # Material 3 light + dark
│   │   │   │   ├── app_colors.dart         # color tokens
│   │   │   │   └── app_typography.dart     # text styles
│   │   │   ├── logger/
│   │   │   │   └── app_logger.dart         # wraps `logging` package
│   │   │   ├── error/
│   │   │   │   ├── app_failure.dart        # sealed Failure type
│   │   │   │   └── error_boundary.dart     # ErrorBoundary widget
│   │   │   ├── local_db/
│   │   │   │   └── isar_service.dart       # Isar singleton
│   │   │   ├── firebase/
│   │   │   │   └── firebase_init.dart
│   │   │   └── widgets/
│   │   │       ├── app_button.dart
│   │   │       ├── app_text_field.dart
│   │   │       ├── empty_state.dart
│   │   │       └── loading_skeleton.dart
│   │   ├── features/
│   │   │   ├── auth/
│   │   │   │   ├── domain/
│   │   │   │   │   ├── auth_user.dart
│   │   │   │   │   ├── auth_repository.dart        # interface
│   │   │   │   │   └── auth_failure.dart
│   │   │   │   ├── data/
│   │   │   │   │   ├── firebase_auth_repository.dart
│   │   │   │   │   └── auth_user_dto.dart
│   │   │   │   └── presentation/
│   │   │   │       ├── providers/
│   │   │   │       │   └── auth_providers.dart
│   │   │   │       ├── onboarding_screen.dart
│   │   │   │       ├── sign_in_screen.dart
│   │   │   │       └── widgets/
│   │   │   │           ├── google_sign_in_button.dart
│   │   │   │           ├── apple_sign_in_button.dart
│   │   │   │           └── email_sign_in_form.dart
│   │   │   ├── home/
│   │   │   │   └── presentation/
│   │   │   │       └── home_screen.dart
│   │   │   ├── library/
│   │   │   │   ├── domain/
│   │   │   │   │   ├── deck.dart
│   │   │   │   │   └── deck_repository.dart
│   │   │   │   ├── data/
│   │   │   │   │   ├── firestore_deck_repository.dart
│   │   │   │   │   └── deck_dto.dart
│   │   │   │   └── presentation/
│   │   │   │       ├── providers/
│   │   │   │       │   └── deck_providers.dart
│   │   │   │       ├── library_screen.dart
│   │   │   │       ├── deck_detail_screen.dart
│   │   │   │       ├── create_deck_screen.dart
│   │   │   │       └── widgets/
│   │   │   │           └── deck_card.dart
│   │   │   ├── card_editor/
│   │   │   │   ├── domain/
│   │   │   │   │   ├── card.dart                   # Card entity (qa/cloze/mcq)
│   │   │   │   │   └── card_repository.dart
│   │   │   │   ├── data/
│   │   │   │   │   ├── firestore_card_repository.dart
│   │   │   │   │   └── card_dto.dart
│   │   │   │   └── presentation/
│   │   │   │       ├── providers/
│   │   │   │       │   └── card_providers.dart
│   │   │   │       ├── add_card_screen.dart
│   │   │   │       └── widgets/
│   │   │   │           ├── qa_card_form.dart
│   │   │   │           ├── cloze_card_form.dart
│   │   │   │           └── mcq_card_form.dart
│   │   │   ├── ingest/
│   │   │   │   ├── domain/
│   │   │   │   │   ├── document.dart
│   │   │   │   │   └── document_repository.dart
│   │   │   │   ├── data/
│   │   │   │   │   ├── firestore_document_repository.dart
│   │   │   │   │   └── document_dto.dart
│   │   │   │   └── presentation/
│   │   │   │       ├── providers/
│   │   │   │       │   └── ingest_providers.dart
│   │   │   │       ├── ingest_sheet.dart
│   │   │   │       └── upload_progress_screen.dart
│   │   │   └── settings/
│   │   │       └── presentation/
│   │   │           └── settings_screen.dart
│   │   └── l10n/
│   │       ├── app_en.arb
│   │       └── app_vi.arb
│   ├── test/
│   │   ├── core/
│   │   ├── features/
│   │   │   ├── auth/
│   │   │   ├── library/
│   │   │   └── card_editor/
│   │   └── helpers/
│   │       └── test_app.dart
│   └── integration_test/
│       └── auth_flow_test.dart
├── docs/
│   ├── specs/
│   ├── plans/
│   └── decisions/
└── firestore.rules
```

---

## Test strategy

- **Pure logic** (mappers, validators, FSRS later, etc): unit test, no Flutter binding
- **Repository**: integration test với Firebase Emulator Suite
- **UI widget**: widget test cho component reusable, golden test cho theme
- **Flow**: integration_test (1 happy path: launch → sign-in → create deck → add card)

CI chỉ chạy unit + widget test (không integration với emulator vì setup phức tạp). Integration chạy local trước commit lớn.

---

# Phase 0 — Project bootstrap (2–3 ngày)

### Task 0.1: Init repo + monorepo skeleton

**Files:**
- Create: `D:\mobile\.gitignore`
- Create: `D:\mobile\README.md`
- Create: `D:\mobile\docs\decisions\` (empty for ADRs later)

- [ ] **Step 1: Init git**

```bash
cd /d/mobile
git init -b main
```

- [ ] **Step 2: Tạo `.gitignore`** (combo Flutter + Python + IDE)

```gitignore
# Flutter
**/.dart_tool/
**/.flutter-plugins
**/.flutter-plugins-dependencies
**/.packages
**/.pub-cache/
**/.pub/
**/build/
**/pubspec.lock
**/*.iml
**/.idea/
**/.vscode/

# Generated
**/*.g.dart
**/*.freezed.dart
**/*.gen.dart
**/*.config.dart

# Firebase
**/firebase_options.dart
**/google-services.json
**/GoogleService-Info.plist
.firebaserc
.firebase/

# Env
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Backend (later)
backend/__pycache__/
backend/.venv/
backend/*.pyc
```

- [ ] **Step 3: README skeleton**

`README.md`:
```markdown
# Zeno — AI Learning Companion

Mobile-first AI flashcard app with spaced repetition. See [spec](docs/specs/2026-05-08-ai-learning-companion-design.md).

## Status
Phase: V1.0 Foundation (in progress)

## Setup
```bash
cd app
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter run
```

## Stack
Flutter • Riverpod • Firebase • FastAPI (later) • Gemini Flash + Claude Haiku
```

- [ ] **Step 4: Commit**

```bash
git add .gitignore README.md
git commit -m "chore: init monorepo skeleton"
```

---

### Task 0.2: Init Flutter app

**Files:**
- Create: `app/` (via `flutter create`)
- Modify: `app/pubspec.yaml`
- Modify: `app/analysis_options.yaml`

- [ ] **Step 1: Tạo Flutter app**

```bash
cd /d/mobile
flutter create --org app.zeno --project-name zeno --platforms=android,ios app
```

- [ ] **Step 2: Add dependencies vào `app/pubspec.yaml`**

```yaml
name: zeno
description: AI Learning Companion
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.4.0
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State + DI
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Routing
  go_router: ^14.2.0

  # Models / serialization
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

  # Firebase
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  cloud_firestore: ^5.2.1
  firebase_storage: ^12.1.3
  firebase_app_check: ^0.3.1+4
  firebase_analytics: ^11.2.1
  firebase_crashlytics: ^4.0.4

  # Auth providers
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.1

  # Local DB
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.1.4

  # Network / files
  dio: ^5.5.0+1
  file_picker: ^8.1.2

  # UI
  flutter_animate: ^4.5.0
  google_fonts: ^6.2.1
  cached_network_image: ^3.4.0

  # Misc
  intl: ^0.19.0
  logging: ^1.2.0
  uuid: ^4.4.2
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  build_runner: ^2.4.11
  riverpod_generator: ^2.4.3
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  isar_generator: ^3.1.0+1
  custom_lint: ^0.6.4
  riverpod_lint: ^2.3.13
  very_good_analysis: ^6.0.0
  mocktail: ^1.0.4
  fake_cloud_firestore: ^3.0.3
  firebase_auth_mocks: ^0.13.0

flutter:
  uses-material-design: true
  generate: true
```

- [ ] **Step 3: Strict lint rules — `app/analysis_options.yaml`**

```yaml
include: package:very_good_analysis/analysis_options.yaml

analyzer:
  plugins:
    - custom_lint
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'
    - '**/firebase_options.dart'
  errors:
    invalid_annotation_target: ignore
```

- [ ] **Step 4: Run pub get**

```bash
cd app
flutter pub get
```

Expected: tất cả resolve, không có conflict.

- [ ] **Step 5: Smoke test default app**

```bash
flutter run -d <emulator-id>
```

Expected: counter app chạy.

- [ ] **Step 6: Commit**

```bash
cd /d/mobile
git add app/
git commit -m "chore(app): init flutter app with deps"
```

---

### Task 0.3: Setup Firebase project

**Files:**
- Create: Firebase project on console (manual)
- Create: `app/lib/firebase_options.dart` (via FlutterFire CLI)
- Create: `firestore.rules`

- [ ] **Step 1: Tạo Firebase project trên console**

1. https://console.firebase.google.com → Add project → tên `zeno-dev`
2. Bật Google Analytics (default account OK)
3. Trong project: bật **Authentication** → Sign-in providers: Google, Apple, Email link
4. Bật **Firestore** → location `asia-southeast1` (Singapore) — KHÔNG đổi được sau
5. Bật **Storage** → cùng location
6. Bật **App Check** (optional cho dev, bắt buộc trước launch)

- [ ] **Step 2: Cài FlutterFire CLI**

```bash
dart pub global activate flutterfire_cli
firebase login
```

- [ ] **Step 3: Configure**

```bash
cd app
flutterfire configure --project=zeno-dev
```

Chọn android + ios. Sẽ tạo `firebase_options.dart` + `google-services.json` + `GoogleService-Info.plist`.

- [ ] **Step 4: Khởi tạo Firebase trong `main.dart`**

`app/lib/core/firebase/firebase_init.dart`:
```dart
import 'package:firebase_core/firebase_core.dart';
import 'package:firebase_app_check/firebase_app_check.dart';
import 'package:firebase_crashlytics/firebase_crashlytics.dart';
import 'package:flutter/foundation.dart';
import '../../firebase_options.dart';

Future<void> initFirebase() async {
  await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);

  await FirebaseAppCheck.instance.activate(
    androidProvider: kDebugMode
        ? AndroidProvider.debug
        : AndroidProvider.playIntegrity,
    appleProvider: kDebugMode
        ? AppleProvider.debug
        : AppleProvider.deviceCheck,
  );

  if (!kDebugMode) {
    FlutterError.onError = FirebaseCrashlytics.instance.recordFlutterError;
  }
}
```

`app/lib/main.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'app.dart';
import 'core/firebase/firebase_init.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await initFirebase();
  runApp(const ProviderScope(child: ZenoApp()));
}
```

- [ ] **Step 5: Tạo `firestore.rules` ở root**

```
rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

Deploy:
```bash
cd /d/mobile
firebase deploy --only firestore:rules --project zeno-dev
```

- [ ] **Step 6: Commit (LƯU Ý: không commit google-services.json/plist nếu repo public)**

`.gitignore` đã exclude rồi. Các config khác:
```bash
git add app/lib/firebase_options.dart firestore.rules app/lib/core/firebase/ app/lib/main.dart
git commit -m "chore(firebase): init core, app check, crashlytics"
```

---

### Task 0.4: GitHub Actions CI

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: CI workflow**

`.github/workflows/ci.yml`:
```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  flutter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.x'
          cache: true
      - name: Pub get
        working-directory: app
        run: flutter pub get
      - name: Codegen
        working-directory: app
        run: dart run build_runner build --delete-conflicting-outputs
      - name: Analyze
        working-directory: app
        run: flutter analyze
      - name: Test
        working-directory: app
        run: flutter test --coverage
```

- [ ] **Step 2: Push & verify**

```bash
git add .github/
git commit -m "ci: add github actions for flutter"
# Push sau khi tạo remote repo
```

---

# Phase V1.0a — Core scaffolding (2 ngày)

### Task 1.1: Theme + design tokens

**Files:**
- Create: `app/lib/core/theme/app_colors.dart`
- Create: `app/lib/core/theme/app_typography.dart`
- Create: `app/lib/core/theme/app_theme.dart`

- [ ] **Step 1: Color tokens**

`app/lib/core/theme/app_colors.dart`:
```dart
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const seed = Color(0xFF6366F1); // indigo-500

  // Semantic - light
  static const lightSurface = Color(0xFFFAFAFA);
  static const lightSurfaceVariant = Color(0xFFF1F5F9);
  static const lightOnSurface = Color(0xFF0F172A);

  // Semantic - dark
  static const darkSurface = Color(0xFF0B0F19);
  static const darkSurfaceVariant = Color(0xFF1E293B);
  static const darkOnSurface = Color(0xFFE2E8F0);

  // Status
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
}
```

- [ ] **Step 2: Typography (Inter + Be Vietnam Pro)**

`app/lib/core/theme/app_typography.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

class AppTypography {
  AppTypography._();

  static TextTheme textTheme(Brightness brightness) {
    final base = brightness == Brightness.light
        ? Typography.blackMountainView
        : Typography.whiteMountainView;
    return GoogleFonts.beVietnamProTextTheme(base).copyWith(
      displayLarge: GoogleFonts.beVietnamPro(fontWeight: FontWeight.w700),
      headlineMedium: GoogleFonts.beVietnamPro(fontWeight: FontWeight.w600),
      titleLarge: GoogleFonts.beVietnamPro(fontWeight: FontWeight.w600),
      bodyLarge: GoogleFonts.beVietnamPro(fontSize: 16, height: 1.5),
      bodyMedium: GoogleFonts.beVietnamPro(fontSize: 14, height: 1.5),
      labelLarge: GoogleFonts.beVietnamPro(fontWeight: FontWeight.w600),
    );
  }
}
```

- [ ] **Step 3: Theme builder**

`app/lib/core/theme/app_theme.dart`:
```dart
import 'package:flutter/material.dart';
import 'app_colors.dart';
import 'app_typography.dart';

class AppTheme {
  AppTheme._();

  static ThemeData light() {
    final scheme = ColorScheme.fromSeed(
      seedColor: AppColors.seed,
      brightness: Brightness.light,
    );
    return ThemeData(
      useMaterial3: true,
      colorScheme: scheme,
      scaffoldBackgroundColor: AppColors.lightSurface,
      textTheme: AppTypography.textTheme(Brightness.light),
      appBarTheme: const AppBarTheme(centerTitle: false, elevation: 0),
      filledButtonTheme: FilledButtonThemeData(
        style: FilledButton.styleFrom(
          minimumSize: const Size.fromHeight(52),
          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
        ),
      ),
      inputDecorationTheme: InputDecorationTheme(
        filled: true,
        border: OutlineInputBorder(
          borderRadius: BorderRadius.circular(12),
          borderSide: BorderSide.none,
        ),
      ),
    );
  }

  static ThemeData dark() {
    final scheme = ColorScheme.fromSeed(
      seedColor: AppColors.seed,
      brightness: Brightness.dark,
    );
    return ThemeData(
      useMaterial3: true,
      colorScheme: scheme,
      scaffoldBackgroundColor: AppColors.darkSurface,
      textTheme: AppTypography.textTheme(Brightness.dark),
      appBarTheme: const AppBarTheme(centerTitle: false, elevation: 0),
      filledButtonTheme: FilledButtonThemeData(
        style: FilledButton.styleFrom(
          minimumSize: const Size.fromHeight(52),
          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
        ),
      ),
    );
  }
}
```

- [ ] **Step 4: Commit**

```bash
git add app/lib/core/theme/
git commit -m "feat(theme): material 3 expressive theme + tokens"
```

---

### Task 1.2: Routing skeleton

**Files:**
- Create: `app/lib/core/routing/app_router.dart`
- Create: `app/lib/app.dart`
- Create: `app/lib/features/home/presentation/home_screen.dart` (placeholder)

- [ ] **Step 1: Placeholder home screen**

`app/lib/features/home/presentation/home_screen.dart`:
```dart
import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Home placeholder')),
    );
  }
}
```

- [ ] **Step 2: Router**

`app/lib/core/routing/app_router.dart`:
```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import 'package:riverpod_annotation/riverpod_annotation.dart';
import '../../features/home/presentation/home_screen.dart';

part 'app_router.g.dart';

@riverpod
GoRouter appRouter(AppRouterRef ref) {
  return GoRouter(
    initialLocation: '/',
    routes: [
      GoRoute(
        path: '/',
        builder: (context, state) => const HomeScreen(),
      ),
    ],
  );
}
```

(Auth guard sẽ thêm ở Task 2.6)

- [ ] **Step 3: App root widget**

`app/lib/app.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'core/routing/app_router.dart';
import 'core/theme/app_theme.dart';

class ZenoApp extends ConsumerWidget {
  const ZenoApp({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final router = ref.watch(appRouterProvider);
    return MaterialApp.router(
      title: 'Zeno',
      theme: AppTheme.light(),
      darkTheme: AppTheme.dark(),
      themeMode: ThemeMode.system,
      routerConfig: router,
    );
  }
}
```

- [ ] **Step 4: Codegen + run**

```bash
cd app
dart run build_runner build --delete-conflicting-outputs
flutter run
```

Expected: thấy "Home placeholder" text.

- [ ] **Step 5: Commit**

```bash
git add app/lib/
git commit -m "feat(core): app shell with goRouter + theme"
```

---

### Task 1.3: Logger + Failure type

**Files:**
- Create: `app/lib/core/logger/app_logger.dart`
- Create: `app/lib/core/error/app_failure.dart`

- [ ] **Step 1: Logger**

`app/lib/core/logger/app_logger.dart`:
```dart
import 'dart:developer' as developer;
import 'package:logging/logging.dart';

void initLogger() {
  Logger.root.level = Level.ALL;
  Logger.root.onRecord.listen((rec) {
    developer.log(
      rec.message,
      name: rec.loggerName,
      time: rec.time,
      level: rec.level.value,
      error: rec.error,
      stackTrace: rec.stackTrace,
    );
  });
}

Logger appLog(String name) => Logger(name);
```

Update `main.dart` to call `initLogger()` before runApp.

- [ ] **Step 2: Sealed Failure type**

`app/lib/core/error/app_failure.dart`:
```dart
import 'package:freezed_annotation/freezed_annotation.dart';

part 'app_failure.freezed.dart';

@freezed
sealed class AppFailure with _$AppFailure {
  const factory AppFailure.network({String? message}) = NetworkFailure;
  const factory AppFailure.auth({required String code, String? message}) = AuthFailure;
  const factory AppFailure.notFound({String? message}) = NotFoundFailure;
  const factory AppFailure.permission({String? message}) = PermissionFailure;
  const factory AppFailure.unknown({String? message, Object? cause}) = UnknownFailure;
}
```

- [ ] **Step 3: Codegen + commit**

```bash
dart run build_runner build --delete-conflicting-outputs
git add app/lib/core/logger/ app/lib/core/error/
git commit -m "feat(core): logger + sealed AppFailure"
```

---

### Task 1.4: Isar local DB setup

**Files:**
- Create: `app/lib/core/local_db/isar_service.dart`

- [ ] **Step 1: Isar singleton**

```dart
import 'package:isar/isar.dart';
import 'package:path_provider/path_provider.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:riverpod_annotation/riverpod_annotation.dart';

part 'isar_service.g.dart';

@Riverpod(keepAlive: true)
Future<Isar> isar(IsarRef ref) async {
  final dir = await getApplicationDocumentsDirectory();
  return Isar.open(
    [
      // Schema sẽ thêm khi có collection (sau Task 4.x)
    ],
    directory: dir.path,
    name: 'zeno',
  );
}
```

- [ ] **Step 2: Codegen + commit**

```bash
dart run build_runner build --delete-conflicting-outputs
git add app/lib/core/local_db/
git commit -m "feat(core): isar service skeleton"
```

---

### Task 1.5: Reusable widgets

**Files:**
- Create: `app/lib/core/widgets/empty_state.dart`
- Create: `app/lib/core/widgets/loading_skeleton.dart`

- [ ] **Step 1: Empty state widget**

```dart
import 'package:flutter/material.dart';

class EmptyState extends StatelessWidget {
  const EmptyState({
    super.key,
    required this.icon,
    required this.title,
    required this.description,
    this.action,
  });

  final IconData icon;
  final String title;
  final String description;
  final Widget? action;

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    return Center(
      child: Padding(
        padding: const EdgeInsets.all(24),
        child: Column(
          mainAxisSize: MainAxisSize.min,
          children: [
            Icon(icon, size: 56, color: theme.colorScheme.primary),
            const SizedBox(height: 16),
            Text(title, style: theme.textTheme.titleLarge),
            const SizedBox(height: 8),
            Text(
              description,
              style: theme.textTheme.bodyMedium,
              textAlign: TextAlign.center,
            ),
            if (action != null) ...[
              const SizedBox(height: 24),
              action!,
            ],
          ],
        ),
      ),
    );
  }
}
```

- [ ] **Step 2: Skeleton loader**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_animate/flutter_animate.dart';

class LoadingSkeleton extends StatelessWidget {
  const LoadingSkeleton({super.key, this.height = 80, this.borderRadius = 12});

  final double height;
  final double borderRadius;

  @override
  Widget build(BuildContext context) {
    final color = Theme.of(context).colorScheme.surfaceContainerHighest;
    return Container(
      height: height,
      decoration: BoxDecoration(
        color: color,
        borderRadius: BorderRadius.circular(borderRadius),
      ),
    )
        .animate(onPlay: (c) => c.repeat())
        .shimmer(duration: 1500.ms, color: color.withValues(alpha: 0.5));
  }
}
```

- [ ] **Step 3: Widget test**

`app/test/core/widgets/empty_state_test.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:zeno/core/widgets/empty_state.dart';

void main() {
  testWidgets('EmptyState shows title, description and optional action',
      (tester) async {
    await tester.pumpWidget(
      const MaterialApp(
        home: Scaffold(
          body: EmptyState(
            icon: Icons.inbox,
            title: 'No decks',
            description: 'Create your first deck',
            action: Text('Action'),
          ),
        ),
      ),
    );

    expect(find.text('No decks'), findsOneWidget);
    expect(find.text('Create your first deck'), findsOneWidget);
    expect(find.text('Action'), findsOneWidget);
    expect(find.byIcon(Icons.inbox), findsOneWidget);
  });
}
```

```bash
flutter test test/core/widgets/empty_state_test.dart
```

Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add app/lib/core/widgets/ app/test/core/widgets/
git commit -m "feat(core): reusable empty state + skeleton widgets"
```

---

# Phase V1.0b — Auth (3 ngày)

### Task 2.1: Auth domain

**Files:**
- Create: `app/lib/features/auth/domain/auth_user.dart`
- Create: `app/lib/features/auth/domain/auth_repository.dart`

- [ ] **Step 1: AuthUser entity**

```dart
import 'package:freezed_annotation/freezed_annotation.dart';

part 'auth_user.freezed.dart';

@freezed
class AuthUser with _$AuthUser {
  const factory AuthUser({
    required String uid,
    required String email,
    String? displayName,
    String? photoUrl,
    required bool isAnonymous,
  }) = _AuthUser;
}
```

- [ ] **Step 2: Repository interface**

```dart
import 'auth_user.dart';

abstract class AuthRepository {
  Stream<AuthUser?> authStateChanges();
  AuthUser? get currentUser;
  Future<AuthUser> signInWithGoogle();
  Future<AuthUser> signInWithApple();
  Future<void> sendEmailLink(String email);
  Future<AuthUser> completeEmailLink(String email, String link);
  Future<void> signOut();
  Future<void> deleteAccount();
}
```

- [ ] **Step 3: Codegen + commit**

```bash
dart run build_runner build --delete-conflicting-outputs
git add app/lib/features/auth/domain/
git commit -m "feat(auth): domain entity + repository interface"
```

---

### Task 2.2: Firebase Auth implementation (TDD)

**Files:**
- Create: `app/lib/features/auth/data/firebase_auth_repository.dart`
- Create: `app/test/features/auth/firebase_auth_repository_test.dart`

- [ ] **Step 1: Write failing test (Google sign-in happy path)**

```dart
import 'package:firebase_auth_mocks/firebase_auth_mocks.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
import 'package:google_sign_in/google_sign_in.dart';
import 'package:zeno/features/auth/data/firebase_auth_repository.dart';

class MockGoogleSignIn extends Mock implements GoogleSignIn {}
class MockGoogleSignInAccount extends Mock implements GoogleSignInAccount {}
class MockGoogleSignInAuthentication extends Mock implements GoogleSignInAuthentication {}

void main() {
  late MockUser mockUser;
  late MockFirebaseAuth firebaseAuth;
  late MockGoogleSignIn googleSignIn;
  late FirebaseAuthRepository repo;

  setUp(() {
    mockUser = MockUser(
      uid: 'uid-1',
      email: 'thu@example.com',
      displayName: 'Thu',
    );
    firebaseAuth = MockFirebaseAuth(mockUser: mockUser, signedIn: false);
    googleSignIn = MockGoogleSignIn();
    repo = FirebaseAuthRepository(
      firebaseAuth: firebaseAuth,
      googleSignIn: googleSignIn,
    );
  });

  test('signInWithGoogle returns AuthUser on success', () async {
    final account = MockGoogleSignInAccount();
    final auth = MockGoogleSignInAuthentication();
    when(() => googleSignIn.signIn()).thenAnswer((_) async => account);
    when(() => account.authentication).thenAnswer((_) async => auth);
    when(() => auth.idToken).thenReturn('id-token');
    when(() => auth.accessToken).thenReturn('access-token');

    final user = await repo.signInWithGoogle();

    expect(user.uid, 'uid-1');
    expect(user.email, 'thu@example.com');
    expect(user.displayName, 'Thu');
  });

  test('signInWithGoogle throws AuthFailure when user cancels', () async {
    when(() => googleSignIn.signIn()).thenAnswer((_) async => null);
    expect(repo.signInWithGoogle, throwsA(isA<Exception>()));
  });
}
```

- [ ] **Step 2: Run — expect FAIL**

```bash
flutter test test/features/auth/firebase_auth_repository_test.dart
```

Expected: file `firebase_auth_repository.dart` chưa tồn tại → fail.

- [ ] **Step 3: Implement repo**

```dart
import 'package:firebase_auth/firebase_auth.dart' as fb;
import 'package:google_sign_in/google_sign_in.dart';
import 'package:sign_in_with_apple/sign_in_with_apple.dart';
import '../domain/auth_repository.dart';
import '../domain/auth_user.dart';
import '../../../core/error/app_failure.dart';

class FirebaseAuthRepository implements AuthRepository {
  FirebaseAuthRepository({
    required fb.FirebaseAuth firebaseAuth,
    required GoogleSignIn googleSignIn,
  })  : _auth = firebaseAuth,
        _googleSignIn = googleSignIn;

  final fb.FirebaseAuth _auth;
  final GoogleSignIn _googleSignIn;

  @override
  Stream<AuthUser?> authStateChanges() =>
      _auth.authStateChanges().map(_mapUser);

  @override
  AuthUser? get currentUser => _mapUser(_auth.currentUser);

  AuthUser? _mapUser(fb.User? u) {
    if (u == null) return null;
    return AuthUser(
      uid: u.uid,
      email: u.email ?? '',
      displayName: u.displayName,
      photoUrl: u.photoURL,
      isAnonymous: u.isAnonymous,
    );
  }

  @override
  Future<AuthUser> signInWithGoogle() async {
    final googleUser = await _googleSignIn.signIn();
    if (googleUser == null) {
      throw const AppFailure.auth(code: 'cancelled', message: 'User cancelled');
    }
    final googleAuth = await googleUser.authentication;
    final cred = fb.GoogleAuthProvider.credential(
      idToken: googleAuth.idToken,
      accessToken: googleAuth.accessToken,
    );
    final result = await _auth.signInWithCredential(cred);
    return _mapUser(result.user)!;
  }

  @override
  Future<AuthUser> signInWithApple() async {
    final apple = await SignInWithApple.getAppleIDCredential(
      scopes: [AppleIDAuthorizationScopes.email, AppleIDAuthorizationScopes.fullName],
    );
    final cred = fb.OAuthProvider('apple.com').credential(
      idToken: apple.identityToken,
      accessToken: apple.authorizationCode,
    );
    final result = await _auth.signInWithCredential(cred);
    return _mapUser(result.user)!;
  }

  @override
  Future<void> sendEmailLink(String email) async {
    await _auth.sendSignInLinkToEmail(
      email: email,
      actionCodeSettings: fb.ActionCodeSettings(
        url: 'https://zeno-dev.firebaseapp.com',
        handleCodeInApp: true,
        androidPackageName: 'app.zeno',
        iOSBundleId: 'app.zeno',
      ),
    );
  }

  @override
  Future<AuthUser> completeEmailLink(String email, String link) async {
    final result = await _auth.signInWithEmailLink(email: email, emailLink: link);
    return _mapUser(result.user)!;
  }

  @override
  Future<void> signOut() async {
    await Future.wait([_auth.signOut(), _googleSignIn.signOut()]);
  }

  @override
  Future<void> deleteAccount() async {
    await _auth.currentUser?.delete();
  }
}
```

- [ ] **Step 4: Run test — expect PASS**

```bash
flutter test test/features/auth/firebase_auth_repository_test.dart
```

- [ ] **Step 5: Commit**

```bash
git add app/lib/features/auth/data/ app/test/features/auth/
git commit -m "feat(auth): firebase auth repository with google/apple/email-link"
```

---

### Task 2.3: Auth providers (Riverpod)

**Files:**
- Create: `app/lib/features/auth/presentation/providers/auth_providers.dart`

- [ ] **Step 1: Providers**

```dart
import 'package:firebase_auth/firebase_auth.dart' as fb;
import 'package:google_sign_in/google_sign_in.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:riverpod_annotation/riverpod_annotation.dart';
import '../../data/firebase_auth_repository.dart';
import '../../domain/auth_repository.dart';
import '../../domain/auth_user.dart';

part 'auth_providers.g.dart';

@Riverpod(keepAlive: true)
AuthRepository authRepository(AuthRepositoryRef ref) {
  return FirebaseAuthRepository(
    firebaseAuth: fb.FirebaseAuth.instance,
    googleSignIn: GoogleSignIn(),
  );
}

@Riverpod(keepAlive: true)
Stream<AuthUser?> authStateChanges(AuthStateChangesRef ref) {
  return ref.watch(authRepositoryProvider).authStateChanges();
}
```

- [ ] **Step 2: Codegen + commit**

```bash
dart run build_runner build --delete-conflicting-outputs
git add app/lib/features/auth/presentation/providers/
git commit -m "feat(auth): riverpod providers"
```

---

### Task 2.4: Sign-in screen

**Files:**
- Create: `app/lib/features/auth/presentation/sign_in_screen.dart`
- Create: `app/lib/features/auth/presentation/widgets/google_sign_in_button.dart`
- Create: `app/lib/features/auth/presentation/widgets/apple_sign_in_button.dart`

- [ ] **Step 1: Buttons**

`google_sign_in_button.dart`:
```dart
import 'package:flutter/material.dart';

class GoogleSignInButton extends StatelessWidget {
  const GoogleSignInButton({super.key, required this.onPressed, this.loading = false});
  final VoidCallback onPressed;
  final bool loading;

  @override
  Widget build(BuildContext context) {
    return OutlinedButton.icon(
      onPressed: loading ? null : onPressed,
      icon: loading
          ? const SizedBox(width: 20, height: 20, child: CircularProgressIndicator(strokeWidth: 2))
          : Image.asset('assets/icons/google.png', width: 20, height: 20, errorBuilder: (_,__,___) => const Icon(Icons.login, size: 20)),
      label: const Text('Tiếp tục với Google'),
      style: OutlinedButton.styleFrom(
        minimumSize: const Size.fromHeight(52),
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
      ),
    );
  }
}
```

(Apple button tương tự với `Icons.apple` để đỡ asset, fix sau.)

- [ ] **Step 2: Sign-in screen**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../../core/error/app_failure.dart';
import 'providers/auth_providers.dart';
import 'widgets/google_sign_in_button.dart';
import 'widgets/apple_sign_in_button.dart';

class SignInScreen extends ConsumerStatefulWidget {
  const SignInScreen({super.key});

  @override
  ConsumerState<SignInScreen> createState() => _SignInScreenState();
}

class _SignInScreenState extends ConsumerState<SignInScreen> {
  bool _googleLoading = false;
  bool _appleLoading = false;

  Future<void> _handleGoogle() async {
    setState(() => _googleLoading = true);
    try {
      await ref.read(authRepositoryProvider).signInWithGoogle();
    } on AppFailure catch (e) {
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text(e.toString())),
        );
      }
    } finally {
      if (mounted) setState(() => _googleLoading = false);
    }
  }

  Future<void> _handleApple() async {
    // similar pattern
  }

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    return Scaffold(
      body: SafeArea(
        child: Padding(
          padding: const EdgeInsets.all(24),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.stretch,
            children: [
              const Spacer(),
              Text('Zeno', style: theme.textTheme.displayLarge),
              const SizedBox(height: 8),
              Text(
                'Học từ bất kỳ thứ gì.\nNhớ mãi mãi.',
                style: theme.textTheme.titleMedium,
              ),
              const Spacer(flex: 2),
              GoogleSignInButton(onPressed: _handleGoogle, loading: _googleLoading),
              const SizedBox(height: 12),
              AppleSignInButton(onPressed: _handleApple, loading: _appleLoading),
              const SizedBox(height: 24),
              Text(
                'Bằng việc đăng nhập, bạn đồng ý với\nĐiều khoản & Chính sách bảo mật',
                style: theme.textTheme.bodySmall,
                textAlign: TextAlign.center,
              ),
              const SizedBox(height: 16),
            ],
          ),
        ),
      ),
    );
  }
}
```

- [ ] **Step 3: Test trên emulator**

Cần config Google Sign-In: SHA-1 fingerprint cho debug keystore — `cd android && ./gradlew signingReport`. Add SHA-1 vào Firebase Console → Project Settings → Android app.

- [ ] **Step 4: Commit**

```bash
git add app/lib/features/auth/presentation/
git commit -m "feat(auth): sign-in screen with google/apple buttons"
```

---

### Task 2.5: Auth guard + routing integration

**Files:**
- Modify: `app/lib/core/routing/app_router.dart`

- [ ] **Step 1: Add redirect logic**

```dart
@riverpod
GoRouter appRouter(AppRouterRef ref) {
  final authState = ref.watch(authStateChangesProvider);

  return GoRouter(
    initialLocation: '/',
    refreshListenable: GoRouterRefreshStream(authState.asStream()),
    redirect: (context, state) {
      final loggedIn = authState.value != null;
      final atSignIn = state.matchedLocation == '/sign-in';

      if (!loggedIn && !atSignIn) return '/sign-in';
      if (loggedIn && atSignIn) return '/';
      return null;
    },
    routes: [
      GoRoute(path: '/sign-in', builder: (_, __) => const SignInScreen()),
      GoRoute(path: '/', builder: (_, __) => const HomeScreen()),
    ],
  );
}

class GoRouterRefreshStream extends ChangeNotifier {
  GoRouterRefreshStream(Stream<dynamic> stream) {
    notifyListeners();
    _sub = stream.asBroadcastStream().listen((_) => notifyListeners());
  }
  late final StreamSubscription<dynamic> _sub;
  @override
  void dispose() {
    _sub.cancel();
    super.dispose();
  }
}
```

- [ ] **Step 2: Test full flow**

```bash
flutter run
```

Expected: launch → sign-in screen → tap Google → choose account → land home.

- [ ] **Step 3: Commit**

```bash
git add app/lib/core/routing/
git commit -m "feat(routing): auth guard with goRouter redirect"
```

---

### Task 2.6: User profile bootstrap (Firestore)

**Files:**
- Modify: `app/lib/features/auth/data/firebase_auth_repository.dart` (add Firestore write on first sign-in)

- [ ] **Step 1: Inject Firestore + bootstrap user doc**

Update repository to write `users/{uid}` doc với defaults sau lần sign-in đầu:

```dart
Future<void> _bootstrapUser(fb.User u) async {
  final docRef = _firestore.collection('users').doc(u.uid);
  final snap = await docRef.get();
  if (!snap.exists) {
    await docRef.set({
      'displayName': u.displayName,
      'email': u.email,
      'photoURL': u.photoURL,
      'createdAt': FieldValue.serverTimestamp(),
      'settings': {
        'reviewTime': '07:00',
        'dailyNewCardLimit': 20,
        'theme': 'system',
        'locale': 'vi',
      },
      'stats': {'streak': 0, 'totalReviews': 0},
    });
  }
}
```

Gọi `_bootstrapUser` sau mỗi sign-in success.

- [ ] **Step 2: Test trên emulator → check Firestore console có doc**

- [ ] **Step 3: Commit**

```bash
git add app/lib/features/auth/data/
git commit -m "feat(auth): bootstrap user firestore doc on first sign-in"
```

---

# Phase V1.0c — Library / Deck CRUD (3 ngày)

### Task 3.1: Deck domain + DTO

**Files:**
- Create: `app/lib/features/library/domain/deck.dart`
- Create: `app/lib/features/library/data/deck_dto.dart`
- Create: `app/lib/features/library/domain/deck_repository.dart`

- [ ] **Step 1: Deck entity**

```dart
@freezed
class Deck with _$Deck {
  const factory Deck({
    required String id,
    required String title,
    String? description,
    @Default([]) List<String> tags,
    @Default('indigo') String coverColor,
    @Default(0) int cardCount,
    @Default(0) int dueCount,
    required DateTime createdAt,
    required DateTime updatedAt,
  }) = _Deck;
}
```

- [ ] **Step 2: DTO + mapper**

```dart
class DeckDto {
  static Deck fromFirestore(DocumentSnapshot snap) {
    final d = snap.data() as Map<String, dynamic>;
    return Deck(
      id: snap.id,
      title: d['title'] as String,
      description: d['description'] as String?,
      tags: List<String>.from(d['tags'] ?? []),
      coverColor: d['coverColor'] as String? ?? 'indigo',
      cardCount: d['cardCount'] as int? ?? 0,
      dueCount: d['dueCount'] as int? ?? 0,
      createdAt: (d['createdAt'] as Timestamp).toDate(),
      updatedAt: (d['updatedAt'] as Timestamp).toDate(),
    );
  }

  static Map<String, dynamic> toFirestore(Deck d) => {
        'title': d.title,
        'description': d.description,
        'tags': d.tags,
        'coverColor': d.coverColor,
        'cardCount': d.cardCount,
        'dueCount': d.dueCount,
        'createdAt': Timestamp.fromDate(d.createdAt),
        'updatedAt': Timestamp.fromDate(d.updatedAt),
      };
}
```

- [ ] **Step 3: Repository interface**

```dart
abstract class DeckRepository {
  Stream<List<Deck>> watchDecks();
  Future<Deck> getDeck(String id);
  Future<Deck> createDeck({required String title, String? description});
  Future<void> updateDeck(Deck deck);
  Future<void> deleteDeck(String id);
}
```

- [ ] **Step 4: Codegen + commit**

```bash
dart run build_runner build --delete-conflicting-outputs
git add app/lib/features/library/
git commit -m "feat(library): deck domain + dto"
```

---

### Task 3.2: Firestore deck repository (TDD)

**Files:**
- Create: `app/lib/features/library/data/firestore_deck_repository.dart`
- Create: `app/test/features/library/firestore_deck_repository_test.dart`

- [ ] **Step 1: Failing test với fake_cloud_firestore**

```dart
import 'package:fake_cloud_firestore/fake_cloud_firestore.dart';
import 'package:firebase_auth_mocks/firebase_auth_mocks.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:zeno/features/library/data/firestore_deck_repository.dart';

void main() {
  late FakeFirebaseFirestore firestore;
  late MockFirebaseAuth auth;
  late FirestoreDeckRepository repo;

  setUp(() {
    firestore = FakeFirebaseFirestore();
    auth = MockFirebaseAuth(mockUser: MockUser(uid: 'u1'), signedIn: true);
    repo = FirestoreDeckRepository(firestore: firestore, auth: auth);
  });

  test('createDeck writes to users/u1/decks', () async {
    final deck = await repo.createDeck(title: 'Sinh học 12');
    expect(deck.title, 'Sinh học 12');
    final snap = await firestore.collection('users/u1/decks').get();
    expect(snap.docs.length, 1);
  });

  test('watchDecks emits sorted by updatedAt desc', () async {
    await repo.createDeck(title: 'A');
    await Future<void>.delayed(const Duration(milliseconds: 10));
    await repo.createDeck(title: 'B');
    final decks = await repo.watchDecks().first;
    expect(decks.first.title, 'B');
  });

  test('deleteDeck removes doc', () async {
    final d = await repo.createDeck(title: 'X');
    await repo.deleteDeck(d.id);
    final snap = await firestore.collection('users/u1/decks').doc(d.id).get();
    expect(snap.exists, false);
  });
}
```

- [ ] **Step 2: Run — expect FAIL**

```bash
flutter test test/features/library/firestore_deck_repository_test.dart
```

- [ ] **Step 3: Implement**

```dart
class FirestoreDeckRepository implements DeckRepository {
  FirestoreDeckRepository({
    required FirebaseFirestore firestore,
    required FirebaseAuth auth,
  })  : _firestore = firestore,
        _auth = auth;

  final FirebaseFirestore _firestore;
  final FirebaseAuth _auth;

  CollectionReference<Map<String, dynamic>> get _decks {
    final uid = _auth.currentUser!.uid;
    return _firestore.collection('users/$uid/decks');
  }

  @override
  Stream<List<Deck>> watchDecks() {
    return _decks
        .orderBy('updatedAt', descending: true)
        .snapshots()
        .map((s) => s.docs.map(DeckDto.fromFirestore).toList());
  }

  @override
  Future<Deck> getDeck(String id) async {
    final snap = await _decks.doc(id).get();
    if (!snap.exists) throw const AppFailure.notFound();
    return DeckDto.fromFirestore(snap);
  }

  @override
  Future<Deck> createDeck({required String title, String? description}) async {
    final now = DateTime.now();
    final ref = _decks.doc();
    final deck = Deck(
      id: ref.id,
      title: title,
      description: description,
      createdAt: now,
      updatedAt: now,
    );
    await ref.set(DeckDto.toFirestore(deck));
    return deck;
  }

  @override
  Future<void> updateDeck(Deck deck) async {
    await _decks.doc(deck.id).update(
          DeckDto.toFirestore(deck.copyWith(updatedAt: DateTime.now())),
        );
  }

  @override
  Future<void> deleteDeck(String id) async {
    await _decks.doc(id).delete();
  }
}
```

- [ ] **Step 4: Run — expect PASS**

- [ ] **Step 5: Commit**

```bash
git add app/lib/features/library/data/ app/test/features/library/
git commit -m "feat(library): firestore deck repository + tests"
```

---

### Task 3.3: Library screen + providers

**Files:**
- Create: `app/lib/features/library/presentation/providers/deck_providers.dart`
- Create: `app/lib/features/library/presentation/library_screen.dart`
- Create: `app/lib/features/library/presentation/widgets/deck_card.dart`
- Modify: `app/lib/core/routing/app_router.dart` (add bottom nav scaffold)

- [ ] **Step 1: Providers**

```dart
@Riverpod(keepAlive: true)
DeckRepository deckRepository(DeckRepositoryRef ref) {
  return FirestoreDeckRepository(
    firestore: FirebaseFirestore.instance,
    auth: FirebaseAuth.instance,
  );
}

@riverpod
Stream<List<Deck>> deckList(DeckListRef ref) {
  return ref.watch(deckRepositoryProvider).watchDecks();
}
```

- [ ] **Step 2: DeckCard widget**

```dart
class DeckCard extends StatelessWidget {
  const DeckCard({super.key, required this.deck, this.onTap});
  final Deck deck;
  final VoidCallback? onTap;

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    return Card(
      child: InkWell(
        onTap: onTap,
        borderRadius: BorderRadius.circular(12),
        child: Padding(
          padding: const EdgeInsets.all(16),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text(deck.title, style: theme.textTheme.titleMedium),
              if (deck.description != null) ...[
                const SizedBox(height: 4),
                Text(deck.description!, style: theme.textTheme.bodySmall),
              ],
              const SizedBox(height: 12),
              Row(
                children: [
                  Icon(Icons.layers_outlined, size: 14, color: theme.colorScheme.primary),
                  const SizedBox(width: 4),
                  Text('${deck.cardCount} cards', style: theme.textTheme.bodySmall),
                  if (deck.dueCount > 0) ...[
                    const SizedBox(width: 12),
                    Container(
                      padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 2),
                      decoration: BoxDecoration(
                        color: theme.colorScheme.errorContainer,
                        borderRadius: BorderRadius.circular(8),
                      ),
                      child: Text('${deck.dueCount} due',
                          style: theme.textTheme.labelSmall),
                    ),
                  ],
                ],
              ),
            ],
          ),
        ),
      ),
    );
  }
}
```

- [ ] **Step 3: Library screen**

```dart
class LibraryScreen extends ConsumerWidget {
  const LibraryScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final decks = ref.watch(deckListProvider);
    return Scaffold(
      appBar: AppBar(title: const Text('Library')),
      floatingActionButton: FloatingActionButton.extended(
        onPressed: () => context.push('/decks/new'),
        icon: const Icon(Icons.add),
        label: const Text('New deck'),
      ),
      body: decks.when(
        data: (list) => list.isEmpty
            ? const EmptyState(
                icon: Icons.library_books_outlined,
                title: 'No decks yet',
                description: 'Tạo deck đầu tiên để bắt đầu học',
              )
            : ListView.separated(
                padding: const EdgeInsets.all(16),
                itemCount: list.length,
                separatorBuilder: (_, __) => const SizedBox(height: 12),
                itemBuilder: (_, i) => DeckCard(
                  deck: list[i],
                  onTap: () => context.push('/decks/${list[i].id}'),
                ),
              ),
        loading: () => const _LibrarySkeletonList(),
        error: (e, _) => Center(child: Text('Lỗi: $e')),
      ),
    );
  }
}
```

- [ ] **Step 4: Bottom nav scaffold**

Wrap home/library/review trong `ShellRoute` của GoRouter (xem GoRouter docs `StatefulShellRoute.indexedStack`).

- [ ] **Step 5: Commit**

```bash
dart run build_runner build --delete-conflicting-outputs
git add app/lib/features/library/presentation/ app/lib/core/routing/
git commit -m "feat(library): library screen with deck list + bottom nav"
```

---

### Task 3.4: Create deck screen

**Files:**
- Create: `app/lib/features/library/presentation/create_deck_screen.dart`

- [ ] **Step 1: Form + submit**

```dart
class CreateDeckScreen extends ConsumerStatefulWidget {
  const CreateDeckScreen({super.key});

  @override
  ConsumerState<CreateDeckScreen> createState() => _CreateDeckScreenState();
}

class _CreateDeckScreenState extends ConsumerState<CreateDeckScreen> {
  final _formKey = GlobalKey<FormState>();
  final _titleCtrl = TextEditingController();
  final _descCtrl = TextEditingController();
  bool _saving = false;

  @override
  void dispose() {
    _titleCtrl.dispose();
    _descCtrl.dispose();
    super.dispose();
  }

  Future<void> _save() async {
    if (!_formKey.currentState!.validate()) return;
    setState(() => _saving = true);
    try {
      final deck = await ref.read(deckRepositoryProvider).createDeck(
            title: _titleCtrl.text.trim(),
            description: _descCtrl.text.trim().isEmpty ? null : _descCtrl.text.trim(),
          );
      if (mounted) context.go('/decks/${deck.id}');
    } finally {
      if (mounted) setState(() => _saving = false);
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('New deck')),
      body: Form(
        key: _formKey,
        child: ListView(
          padding: const EdgeInsets.all(24),
          children: [
            TextFormField(
              controller: _titleCtrl,
              decoration: const InputDecoration(labelText: 'Title'),
              validator: (v) => v == null || v.trim().isEmpty ? 'Required' : null,
              autofocus: true,
            ),
            const SizedBox(height: 16),
            TextFormField(
              controller: _descCtrl,
              decoration: const InputDecoration(labelText: 'Description (optional)'),
              maxLines: 3,
            ),
            const SizedBox(height: 32),
            FilledButton(
              onPressed: _saving ? null : _save,
              child: _saving ? const CircularProgressIndicator() : const Text('Create'),
            ),
          ],
        ),
      ),
    );
  }
}
```

- [ ] **Step 2: Add route + commit**

```bash
git add app/lib/features/library/presentation/create_deck_screen.dart app/lib/core/routing/
git commit -m "feat(library): create deck screen"
```

---

### Task 3.5: Deck detail + edit/delete

**Files:**
- Create: `app/lib/features/library/presentation/deck_detail_screen.dart`

- [ ] **Step 1: Detail screen với app bar action menu (edit/delete)**

(Tương tự pattern create — dùng `ref.watch(deckByIdProvider(id))`. Add edit screen reusing form. Delete với confirm dialog.)

- [ ] **Step 2: Commit**

```bash
git commit -am "feat(library): deck detail + edit/delete"
```

---

# Phase V1.0d — Card CRUD thủ công (3 ngày)

### Task 4.1: Card entities (qa/cloze/mcq)

**Files:**
- Create: `app/lib/features/card_editor/domain/card.dart`

- [ ] **Step 1: Sealed Card type**

```dart
@freezed
sealed class FlashCard with _$FlashCard {
  const factory FlashCard.qa({
    required String id,
    required String deckId,
    required String front,
    required String back,
    required DateTime createdAt,
  }) = QaCard;

  const factory FlashCard.cloze({
    required String id,
    required String deckId,
    required String text, // "Mitochondria là {{c1::nhà máy năng lượng}}"
    required DateTime createdAt,
  }) = ClozeCard;

  const factory FlashCard.mcq({
    required String id,
    required String deckId,
    required String question,
    required List<String> options,
    required int correctIndex,
    required DateTime createdAt,
  }) = McqCard;
}
```

- [ ] **Step 2: Codegen + commit**

```bash
git commit -am "feat(cards): sealed FlashCard entity"
```

---

### Task 4.2 — 4.5

(Same pattern: DTO mapper → Firestore repository (TDD) → Riverpod providers → AddCardScreen với 3 form variants → Cards list trong DeckDetailScreen.)

Để tránh plan dài quá, tôi giữ Task 4.2-4.5 dưới dạng **brief**:

- **Task 4.2:** Card DTO + Firestore repository (write `users/{uid}/decks/{deckId}/cards`). TDD với fake_cloud_firestore. Khi tạo card → atomic update `cardCount` trên deck doc (Firestore batch).
- **Task 4.3:** Card providers + AddCardScreen với 3 tab (Q/A, Cloze, MCQ).
- **Task 4.4:** Show card list trong deck detail. List hiển thị icon + front preview.
- **Task 4.5:** Edit/delete card với swipe action.

Mỗi task ~1 ngày. **Mỗi task end với commit.**

---

# Phase V1.0e — Document upload (PDF/URL/text) skeleton (2 ngày)

### Task 5.1: Document entity + Storage upload

**Files:**
- Create: `app/lib/features/ingest/domain/document.dart`
- Create: `app/lib/features/ingest/data/firestore_document_repository.dart`

- [ ] **Step 1: Document entity**

```dart
enum DocumentType { pdf, url, text, image }
enum DocumentStatus { uploading, uploaded, processing, ready, failed }

@freezed
class Document with _$Document {
  const factory Document({
    required String id,
    required String deckId,
    required DocumentType type,
    required String title,
    String? sourceUrl,
    String? storageRef,
    @Default(DocumentStatus.uploading) DocumentStatus status,
    String? errorMessage,
    required DateTime createdAt,
  }) = _Document;
}
```

- [ ] **Step 2: Repository — upload PDF flow**

```dart
class FirestoreDocumentRepository implements DocumentRepository {
  // ...
  @override
  Future<Document> uploadPdf({
    required String deckId,
    required File file,
  }) async {
    final uid = _auth.currentUser!.uid;
    final id = _docs(deckId).doc().id;
    final storagePath = 'users/$uid/documents/$id.pdf';
    final ref = _storage.ref(storagePath);

    final doc = Document(
      id: id,
      deckId: deckId,
      type: DocumentType.pdf,
      title: file.path.split('/').last,
      storageRef: storagePath,
      status: DocumentStatus.uploading,
      createdAt: DateTime.now(),
    );
    await _docs(deckId).doc(id).set(DocumentDto.toFirestore(doc));

    final task = ref.putFile(file);
    await task;
    await _docs(deckId).doc(id).update({'status': 'uploaded'});
    return doc.copyWith(status: DocumentStatus.uploaded);
  }
}
```

(`status: ready` sẽ được set bởi backend khi AI process xong — Plan 2.)

- [ ] **Step 3: Commit**

```bash
git commit -am "feat(ingest): document entity + storage upload"
```

---

### Task 5.2: Ingest sheet UI

**Files:**
- Create: `app/lib/features/ingest/presentation/ingest_sheet.dart`

- [ ] **Step 1: Modal bottom sheet với 4 lựa chọn**

```dart
showModalBottomSheet(
  context: context,
  builder: (_) => Column(
    mainAxisSize: MainAxisSize.min,
    children: [
      ListTile(leading: Icon(Icons.picture_as_pdf), title: Text('PDF'), onTap: _pickPdf),
      ListTile(leading: Icon(Icons.link), title: Text('Link'), onTap: _pasteUrl),
      ListTile(leading: Icon(Icons.text_fields), title: Text('Text'), onTap: _pasteText),
      ListTile(leading: Icon(Icons.camera_alt), title: Text('Snap a page'), onTap: _snapImage),
    ],
  ),
);
```

`_pickPdf` dùng `file_picker`:
```dart
final result = await FilePicker.platform.pickFiles(
  type: FileType.custom,
  allowedExtensions: ['pdf'],
);
if (result != null) {
  final file = File(result.files.single.path!);
  await ref.read(documentRepositoryProvider).uploadPdf(deckId: deckId, file: file);
}
```

- [ ] **Step 2: Show progress + handle error**

(Listen `Reference.putFile().snapshotEvents` cho progress %.)

- [ ] **Step 3: Commit**

```bash
git commit -am "feat(ingest): ingest sheet UI with PDF picker + storage upload"
```

---

### Task 5.3: Document list trong deck detail

**Files:**
- Modify: `app/lib/features/library/presentation/deck_detail_screen.dart`

- [ ] **Step 1: Tab "Sources" hiển thị documents**

(Mỗi document show icon + title + status pill.)

- [ ] **Step 2: Commit**

```bash
git commit -am "feat(library): show documents in deck detail"
```

---

# Phase V1.0f — Polish & integration test (1 ngày)

### Task 6.1: Integration test — happy path

**Files:**
- Create: `app/integration_test/v1_0_smoke_test.dart`

- [ ] **Step 1: Flow test**

```dart
testWidgets('V1.0 smoke: sign-in → create deck → add card → upload pdf', (tester) async {
  // 1. Launch
  // 2. Sign in (use a test account or skip with mocked auth)
  // 3. Tap FAB "New deck" → enter title → save
  // 4. Tap deck → tap "Add card" → fill QA → save
  // 5. Tap "Upload PDF" → assert status changes to "uploaded"
});
```

(Phải dùng Firebase Emulator Suite cho test này.)

- [ ] **Step 2: Manual smoke trên 1 device thật (Android) + 1 emulator iOS nếu có**

- [ ] **Step 3: Update README với hướng dẫn run**

- [ ] **Step 4: Tag release**

```bash
git tag v0.1.0-foundation
git push --tags
```

---

# Definition of done — Plan 1

- [x] App build success Android + iOS
- [x] User flow: sign in → create deck → add cards → upload PDF — chạy end-to-end
- [x] Firestore rules deployed, App Check active
- [x] Crash-free trên 5 review run
- [x] CI green: analyze + test
- [x] Coverage ≥40% (mostly logic + repos, không cần coverage UI)
- [x] Tag `v0.1.0-foundation`

---

# Notes for next plan (V1.1)

- Backend FastAPI scaffold (Cloud Run + Docker)
- AI gen cards: `/ingest/pdf` → parse PDF → chunk → LLM → return cards
- FSRS algorithm implementation (Dart side)
- Review screen với swipe gesture
- Streak counter logic
- Daily review notification (FCM + local)

Backlog ý tưởng V2/V3 (đừng touch tới khi V1 hoàn thiện): voice mode, knowledge graph, social, Anki export, premium tier.

---

## Reference & gotchas

- **Riverpod codegen** lúc thay file → luôn `dart run build_runner watch` background hoặc rerun `build`
- **Firebase Auth Apple** trên Android cần OAuth flow web, hơi phức tạp — defer tới khi cần
- **Email link sign-in** cần host Firebase Hosting hoặc dynamic link — defer cũng OK
- **App Check** dev mode: dùng debug provider; nhớ thêm debug token vào console
- **Firestore index**: `orderBy('updatedAt')` cần index — Firestore tự gen, click link trong console khi error
- **Isar v4** dependency hơi nặng (~30MB binary), cân nhắc nếu APK size critical
